#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(){
     int num = 1024

	while(num ! = 0)
	{
		nu =
			putchar(c);
		}
	}
	putchar('\n');
	return (0);
}
